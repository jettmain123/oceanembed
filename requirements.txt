# Core (required)
numpy>=1.24
scipy>=1.10
pandas>=2.0
xarray>=2023.1
netCDF4>=1.6
pyyaml>=6.0
scikit-learn>=1.3
matplotlib>=3.7

# Model backend -- optional. Without torch the NumPy fallback trains the model.
# CPU-only install:  pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Table export + demo
pyarrow>=12.0
streamlit>=1.28

# Real-data download helpers (only needed when pulling real products)
# copernicusmarine>=1.3
